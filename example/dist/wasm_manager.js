export async function loadWasmModule(moduleName) {
    // Dynamically import the Emscripten glue code generated by dev.js
    const { default: initModule } = await import(`./${moduleName}.js`);
    
    // Initialize and return the Emscripten Module object
    return await initModule();
}
